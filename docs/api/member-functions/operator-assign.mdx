---
sidebar_position: 1
sidebar_label: operator=
---
# `fixstr::basic_fixed_string::operator=`

import CppOverload from '../../components/CppOverload';
import CppOverloadList from '../../components/CppOverloadList';
import LinkButton from '../../components/LinkButton';
import CodeBlock from '@theme/CodeBlock';
import Overload1 from '!!raw-loader!.//operator-assign/1.cpp';
import Example from '!!raw-loader!.//operator-assign/example.cpp';

<CppOverloadList>
    <CppOverload num={1} code={Overload1} />
</CppOverloadList>

Replaces the contents with a copy of `array`.

## Parameters

* `array` - reference to an array of characters to use as source to initialize the fixed string with

## Complexity

Linear in `N`

## Example

<LinkButton link="https://godbolt.org/z/da78q9xc8">Run this code!</LinkButton>
<CodeBlock className="language-cpp">{Example}</CodeBlock>
Output:

```
Hello, World!
```
